#include <iostream>

using namespace std;

int n, m;
string str[10005];
int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);

	cin >> n;
	while (n--) {
		cin >> m;
		for(int i = 0; i < m; i++)
			cin >> str[i];
		
	}
}