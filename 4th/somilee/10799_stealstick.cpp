#include <iostream>
#include <queue>
#include <string>

using namespace std;

int main(void)
{
	string str;

	cin >> str;
	queue<int> q;
	int i = 0;
	while(str[i])
	{
		if (str[i] == "(")


		cout << str[i] << "\n";
		i ++;
	}

	return (0);
}
